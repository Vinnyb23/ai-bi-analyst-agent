# CPU-only PyTorch build (small, no CUDA) -- needed as a sentence-transformers
# dependency for local embeddings. See https://pytorch.org/get-started/locally/
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.14.0

pandas==3.0.5
numpy==2.5.2

# RAG: local embeddings (no API key) + vector store + chunking
sentence-transformers==6.0.1
chromadb==1.5.9
langchain-text-splitters==1.1.2

# Multi-agent pipeline
langgraph==1.2.11

# LLM providers for generation (text-to-SQL, summarization, report drafting,
# RAG answer synthesis). Only ONE needs a key set -- see .env.example. Every
# module in src/ has a template/keyword-based fallback path so the whole
# pipeline still runs end-to-end with zero API keys configured.
openai==3.7.0
anthropic==1.3.0

# API + front end
Flask==3.1.3
streamlit==1.63.0

python-dotenv==1.2.3

# Dev/test
pytest==9.1.1
